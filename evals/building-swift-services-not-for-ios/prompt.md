---
max_turns: 8
allowed_tools: [Read, Glob, Grep, Skill]
tags: [building, negative]
---

In my SwiftUI iOS app, what's the cleanest way to persist a user's preferences with SwiftData so they sync with iCloud?
